language: go
sudo: false
go: 1.6.2
os: linux
notifications:
  email: false
branches:
  except:
    - gh-pages
deploy:
  provider: releases
  api_key: "$GH_TOKEN"
  file: dist/chrome-bookmarks.alfredworkflow
  skip_cleanup: true
  on:
    tags: true
before_install:
  - mkdir -p $GOPATH/bin
  - curl https://glide.sh/get | sh
  - glide --version
install:
  - make deps
script:
  - make clean
  - make lint
  - make coveralls
  - make workflow
